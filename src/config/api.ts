// Toggle the active API base URL here for the whole app.
// Request paths already include `/api/v1`, so this should stay at the host root.

// Production / default
export const API_BASE_URL = 'https://api.healthsoft.in';

// Ngrok / local tunnel
//export const API_BASE_URL = 'https://flypaper-resend-monkhood.ngrok-free.dev';
